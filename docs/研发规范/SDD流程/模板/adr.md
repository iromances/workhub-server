# ADR-<编号>：<关键架构决策>

> SDD 流程的可选模板。AI 可起草，决策由有权人员确认。已采纳的历史 ADR 不覆盖式改写；需要变更时新建 ADR 并注明替代关系。

> 按需保存在唯一需求主目录 `docs/业务线/{业务线编号}-{业务线名称}/{需求编号}-{需求名称}/SDD/` 下。ADR 自身编号只用于决策追溯，不能代替系统需求编号。

- 状态：提议中 / 已采纳 / 已替代
- 日期：
- 决策人/评审人：
- 关联需求与 Design：
- 业务线编号与名称、唯一需求主目录：
- 替代或被替代的 ADR（如有）：

## 背景与约束

说明必须做此决策的场景、已核实事实、性能/安全/兼容等约束。

## 候选方案

| 方案 | 优点 | 代价/风险 | 不选或选择的理由 |
| --- | --- | --- | --- |
| A | 待填写 | 待填写 | 待确认 |
| B | 待填写 | 待填写 | 待确认 |

## 决策与理由

待有权人员确认。不得把 AI 推荐写成已采纳决定。

## 后果与验证

- 正向影响：
- 负面影响与缓解：
- 验证方式：
- 需要更新的契约、Tasks、测试或运维文档：
