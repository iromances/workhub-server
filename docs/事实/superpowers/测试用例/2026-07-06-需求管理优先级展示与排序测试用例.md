# 自动化测试用例：需求管理优先级展示与排序

## 1. 需求信息

- 需求名称：需求管理增加优先级展示、修改和排序
- 研发方案文件：`docs/事实/superpowers/设计方案/2026-07-06-需求管理优先级展示与排序设计方案.md`
- 测试用例文件路径：`docs/事实/superpowers/测试用例/2026-07-06-需求管理优先级展示与排序测试用例.md`
- 涉及系统：`workhub-server`、`workhub-web`
- 涉及模块：需求管理列表
- 编写时间：2026-07-06
- 编写人或代理：Codex

## 2. 测试范围

- 核心场景：列表返回并展示优先级；操作列修改优先级；同状态下按高、中、低排序。
- 边界场景：优先级为空时显示 `-` 且排序靠后；保存相同优先级不重复写历史。
- 异常场景：非法优先级被后端拒绝。
- 不纳入自动化测试的范围及原因：真实浏览器点击弹窗暂不纳入自动化，当前前端未配置页面级自动化测试；以构建和手工验证替代。

## 3. 测试环境

- 后端环境：本地 Maven 测试环境。
- 前端环境：本地 Node/Vite 构建环境。
- 数据库环境：单元测试使用 mock，不依赖真实数据库。
- 外部依赖：不涉及。
- 测试数据准备方式：单元测试构造不同优先级和需求状态的记录。

## 4. 自动化测试用例

| 用例编号 | 场景类型 | 覆盖目标 | 前置条件 | 执行命令或测试方法 | 预期结果 | 开发完成后执行结果 | 失败原因或备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 核心场景 | `IntakeService.list` 在现有排序基础上追加优先级权重 | 构造同识别状态、同需求状态但优先级不同的记录 | `mvn -pl workhub-service -am -Dtest=IntakeServiceListFilterTest -Dsurefire.failIfNoSpecifiedTests=false test` | 返回顺序为高、中、低、空 | 通过 | Tests run: 11, Failures: 0, Errors: 0 |
| TC-002 | 边界场景 | 优先级排序不覆盖既有需求状态排序 | 构造不同需求状态和优先级记录 | `mvn -pl workhub-service -am -Dtest=IntakeServiceListFilterTest -Dsurefire.failIfNoSpecifiedTests=false test` | 需求状态排序仍优先于优先级排序 | 通过 | Tests run: 11, Failures: 0, Errors: 0 |
| TC-003 | 核心场景 | 修改优先级成功并记录历史 | Mock `IntakeMapper` 和 `IntakeHistoryMapper` | `mvn -pl workhub-service -am -Dtest=IntakeServiceListFilterTest -Dsurefire.failIfNoSpecifiedTests=false test` | 更新为指定优先级，并写入修改历史 | 通过 | Tests run: 11, Failures: 0, Errors: 0 |
| TC-004 | 异常场景 | 非法优先级被拒绝 | 传入 `紧急` | `mvn -pl workhub-service -am -Dtest=IntakeServiceListFilterTest -Dsurefire.failIfNoSpecifiedTests=false test` | 抛出业务异常，不更新数据 | 通过 | Tests run: 11, Failures: 0, Errors: 0 |
| TC-005 | 接口场景 | Controller 暴露优先级修改接口 | Mock `IntakeService` | `mvn -pl workhub-controller -am -Dtest=IntakeControllerTest -Dsurefire.failIfNoSpecifiedTests=false test` | 调用 `updatePriority` 并返回详情 | 通过 | Tests run: 17, Failures: 0, Errors: 0 |
| TC-006 | 前端构建 | 类型和页面模板可构建 | 完成前端列、弹窗、接口接入 | `cd ../workhub-web && npm run build` | 构建通过 | 通过 | `vue-tsc --noEmit && vite build` 成功 |
| TC-007 | 核心场景 | 需求录入默认优先级为中 | 构造上传录入请求，不传优先级 | `mvn -pl workhub-service -am -Dtest=IntakeServiceTest,IntakeServiceListFilterTest -Dsurefire.failIfNoSpecifiedTests=false test` | 插入待整理主记录时 `priority=中` | 通过 | Tests run: 44, Failures: 0, Errors: 0 |

## 5. 执行命令与结果

开发完成后，只填写实际执行过的命令和真实结果。

| 执行时间 | 执行人或代理 | 命令 | 结果 | 关键输出 | 备注 |
|---|---|---|---|---|---|
| 2026-07-06 09:17:50 CST | Codex | `mvn -pl workhub-service -am -Dtest=IntakeServiceListFilterTest -Dsurefire.failIfNoSpecifiedTests=false test` | 通过 | Tests run: 11, Failures: 0, Errors: 0 | 覆盖列表过滤既有用例和新增优先级排序、保存、非法值校验 |
| 2026-07-06 09:17:58 CST | Codex | `mvn -pl workhub-controller -am -Dtest=IntakeControllerTest -Dsurefire.failIfNoSpecifiedTests=false test` | 通过 | Tests run: 17, Failures: 0, Errors: 0 | 覆盖列表字段和优先级保存接口 |
| 2026-07-06 09:18:10 CST | Codex | `cd ../workhub-web && npm run build` | 通过 | `vue-tsc --noEmit && vite build` 成功 | 前端类型和生产构建通过 |
| 2026-07-06 09:18:20 CST | Codex | `mvn -q -DskipTests compile` | 通过 | 命令退出码 0 | 后端全仓编译通过 |
| 2026-07-06 15:24:07 CST | Codex | `mvn -pl workhub-service -am -Dtest=IntakeServiceTest,IntakeServiceListFilterTest -Dsurefire.failIfNoSpecifiedTests=false test` | 通过 | Tests run: 44, Failures: 0, Errors: 0 | 覆盖需求录入默认优先级为中 |

## 6. 未覆盖风险

- 未覆盖场景：登录态下的真实页面点击“修改优先级”并检查网络请求。
- 未覆盖原因：当前前端未配置页面级自动化测试；如本地登录态可用，将补充手工验证。
- 可能影响：真实环境弹窗交互需联调最终确认。
- 后续补充计划：登录 `http://127.0.0.1:9530/demands`，选择一条需求修改优先级，确认列表刷新后展示新值。

## 7. 测试结论

- 是否完成计划自动化测试：已完成后端 Service、Controller 单元测试和前端构建验证。
- 是否存在失败用例：不存在已执行失败用例。
- 是否允许进入交付：允许进入代码交付；真实登录态页面点击建议在联调环境补充确认。
- 结论说明：优先级默认值、排序、保存、非法值校验、接口暴露和前端构建均已通过验证。
