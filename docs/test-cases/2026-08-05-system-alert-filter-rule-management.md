# 系统预警过滤规则页面维护测试用例

## 1. 需求信息

- 需求名称：系统预警过滤规则页面维护
- 研发方案文件：`docs/研发设计/迭代/2026-08-05-system-alert-filter-rule-management.md`
- 涉及系统：workhub-server、workhub-web、MySQL
- 涉及模块：系统预警、ELK 采集、站内通知
- 编写时间：2026-08-05
- 编写人或代理：Codex

## 2. 测试范围

- 核心场景：规则 CRUD、首条优先匹配、三种处理动作、页面维护。
- 边界场景：ANY/ALL、MESSAGE/ALL_TEXT、同优先级、空规则、重复关键词。
- 异常场景：非法枚举、空关键词、重复名称、删除不存在规则。
- 不纳入自动化：真实 MySQL 迁移和生产 ELK 联调。

## 3. 测试环境

- 后端：本地 Maven 测试环境
- 前端：Vite 生产构建与浏览器 demo
- 数据库：Flyway 脚本静态校验，条件允许时本地 MySQL
- 外部依赖：伪造 ELK Source 和通知 Service

## 4. 自动化测试用例

| 编号 | 类型 | 场景 | 预期结果 | 执行结果 | 备注 |
|---|---|---|---|---|---|
| TC-001 | 核心 | ANY 任一关键词命中 | 返回规则动作 | 通过 | 分类器单测覆盖 |
| TC-002 | 核心 | ALL 所有关键词命中 | 全部命中才返回动作 | 通过 | 分类器单测覆盖 |
| TC-003 | 边界 | MESSAGE 不读取堆栈 | 堆栈命中不生效 | 通过 | 分类器单测覆盖 |
| TC-004 | 核心 | ALL_TEXT 读取完整正文 | 堆栈命中生效 | 通过 | 分类器单测覆盖 |
| TC-005 | 核心 | 多规则按优先级首条命中 | 返回最先命中动作 | 通过 | 分类器单测覆盖 |
| TC-006 | 兼容 | 无规则命中 | 默认 SYSTEM_ERROR | 通过 | 分类器单测覆盖 |
| TC-007 | 核心 | IGNORE 采集分流 | 不入库、不通知 | 通过 | 采集器单测覆盖 |
| TC-008 | 核心 | SLOW_SQL 采集分流 | 入库、不通知 | 通过 | 采集器单测覆盖 |
| TC-009 | 核心 | SYSTEM_ERROR 采集分流 | 入库并通知 | 通过 | 采集器单测覆盖 |
| TC-010 | CRUD | 新增规则和关键词 | 按顺序保存并返回 | 通过 | 规则服务单测覆盖 |
| TC-011 | CRUD | 编辑规则替换关键词 | 旧关键词删除，新关键词生效 | 通过 | 规则服务单测覆盖 |
| TC-012 | 异常 | 非法规则参数 | 返回参数异常 | 通过 | 规则服务单测覆盖 |
| TC-013 | 数据 | 初始化现有规则 | 规则与硬编码口径一致 | 部分通过 | 脚本静态核对，未在真实 MySQL 执行 |
| TC-014 | 页面 | 过滤规则页签 | 列表、筛选、抽屉和操作可用 | 通过 | 生产构建与桌面视口浏览器验收通过 |

## 5. 执行命令与结果

| 时间 | 执行人 | 命令或方法 | 结果 | 关键输出 |
|---|---|---|---|---|
| 2026-08-05 | Codex | Maven 定向测试（分类器、采集器、规则服务、事件服务、Controller） | 通过 | 退出码 0 |
| 2026-08-05 | Codex | `npm run build` | 通过 | Vue 类型检查与 Vite 构建成功 |
| 2026-08-05 | Codex | 本地浏览器桌面视口验收 | 通过 | 四个顶层页签、默认最近事件页签、过滤规则筛选/提示/列表/操作均可见 |
| 2026-08-05 | Codex | `mvn -q clean package -DskipTests` | 通过 | 后端完整多模块编译与打包成功 |

## 6. 未覆盖风险

- 未覆盖：真实数据量下迁移耗时、生产 ELK 字段差异、已登录环境写操作。
- 后续：部署前检查 Flyway，部署后核对初始化规则并做一轮只读采集状态验证。

## 7. 测试结论

- 自动化测试：通过
- 失败用例：无
- 是否允许交付：允许；部署后补真实迁移与权限内写操作验证
- 结论：核心规则匹配、采集分流、页面构建和浏览器展示已通过；真实 MySQL 迁移与已登录写操作仍需部署后验证。
