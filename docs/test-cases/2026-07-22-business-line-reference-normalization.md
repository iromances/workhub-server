# 业务线编码引用归一自动化测试用例

## 1. 需求信息

- 需求名称：业务线编码引用归一
- 研发方案文件：`docs/研发设计/迭代/2026-07-22-business-line-reference-normalization.md`
- 测试用例文件路径：`docs/test-cases/2026-07-22-business-line-reference-normalization.md`
- 涉及系统：WorkHub 后端、MySQL
- 涉及模块：项目、需求、研发分析、澄清分析、支付绑定、MCP 业务线上下文
- 编写时间：2026-07-22
- 编写人或代理：Codex

## 2. 测试范围

- 核心场景：只写入业务线编码，查询返回当前名称，改名后所有引用即时变化。
- 边界场景：未归类需求允许空编码；中台系统允许空编码。
- 异常场景：项目和支付绑定留有空编码时迁移应失败。
- 不纳入：真实支付、外部系统调用和前端视觉回归。

## 3. 测试环境

- 后端环境：本地，分支 `codex/dev-20260707`
- 前端环境：本次不改前端
- 数据库环境：本地 WorkHub 配置数据库
- 外部依赖：无
- 测试数据：现有业务线、项目、支付绑定和需求数据；迁移前建立定向备份。

## 4. 自动化测试用例

| 用例编号 | 场景类型 | 覆盖目标 | 前置条件 | 执行命令或测试方法 | 预期结果 | 开发完成后执行结果 | 失败原因或备注 |
|---|---|---|---|---|---|---|---|
| TC-001 | 核心 | 项目/涉及系统/支付只写编码 | 有启用业务线 | Service 单测 + Mapper SQL 复核 | 实体写入不依赖名称字段 | 通过 | 写 SQL 仅包含 `business_line_code` |
| TC-002 | 核心 | 查询关联当前名称 | 业务线已改名 | 本地启动 + SQL 验证 | 所有响应显示新名称 | 通过 | 新代码启动成功，四个编码均查询到新名称 |
| TC-003 | 核心 | JSON 不持久化名称 | 需求和分析 JSON 有效 | SQL 查询 | 库内无顶层 `businessLine`，DAO 读取可补回 | 通过 | 四类 JSON 路径残留均为 0 |
| TC-004 | 边界 | 未归类需求 | 原文本无可识别业务线 | 迁移对账 | 编码为空，原始文本保留 | 通过 | 15 条需求保持未归类 |
| TC-005 | 异常 | 强关联空编码防护 | 项目/支付绑定存在空编码 | 结构与迁移前置校验 | 新写入被拒绝 | 通过 | 强关联表已加检查约束，迁移后空编码和孤儿编码均为 0 |
| TC-006 | 回归 | 受影响 Service | 显式 Byte Buddy agent | Maven 定向及全仓测试 | 用例通过 | 部分通过 | 定向测试全过；全仓 247 项仅 1 项既有运维工时兜底断言失败 |

## 5. 执行命令与结果

| 执行时间 | 执行人或代理 | 命令 | 结果 | 关键输出 | 备注 |
|---|---|---|---|---|---|
| 2026-07-22 | Codex | `mvn -q -DskipTests compile` | 通过 | 全仓编译成功 |  |
| 2026-07-22 | Codex | `mvn -q -DskipTests test-compile` | 通过 | 测试源码编译成功 |  |
| 2026-07-22 | Codex | 受影响 Service 定向测试（显式 `javaagent`） | 通过 | 业务线、支付、分析、MCP 用例全部通过 |  |
| 2026-07-22 | Codex | 迁移 DML 事务试跑后回滚 | 通过 | 项目、成员、业务线系统、支付绑定空编码均0 | 需求仍有15条未归类，符合方案 |
| 2026-07-22 | Codex | 创建7张 `bak_blref_*_20260722_190000` 定向备份表 | 通过 | 被删名称字段及相关 JSON 已备份 | 备份表保留，不参与在线查询 |
| 2026-07-22 | Codex | Flyway `V20260722_190000` | 通过 | `flyway_schema_history.success=1` | 7张在线表名称列已删除 |
| 2026-07-22 | Codex | 迁移后 SQL 对账 | 通过 | 强关联空编码0、全部孤儿编码0、JSON名称残留0 | 仅15条无法识别的历史需求保持空编码 |
| 2026-07-22 | Codex | 新代码 18080 端口启动 | 通过 | Flyway 校验26个迁移，应用启动完成，`/api/system/ping` 返回200 | 业务接口未带登录态按预期返回401 |
| 2026-07-22 | Codex | 全仓测试（显式 `javaagent`，允许本地测试端口） | 部分通过 | 247项，246项通过、1项失败 | 失败为工作区已有运维工时旧JSON兜底断言，与本次业务线改造无关 |

## 6. 未覆盖风险

- 未使用真实登录态逐个调用业务接口，也未在前端页面检查所有展示位置。
- 当前 8080 端口仍是用户先前启动的旧进程，需重启后才会加载本次 Mapper 改造。
- 全仓仍有1项与本需求无关的运维工时旧JSON兜底断言失败。

## 7. 测试结论

- 是否完成计划自动化测试：是，本次影响范围测试已完成。
- 是否存在失败用例：本次需求定向用例无失败；全仓有1个既有非关联失败。
- 是否允许进入交付：是，需重启当前8080旧进程加载新代码。
- 结论说明：数据库迁移、数据对账、应用启动和定向回归均通过。
