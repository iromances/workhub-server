# 钱多多 v2 升级阻塞报告

## 当前状态

- 现有宠物包 `/Users/aslight/.codex/pets/qianduoduo` 未被修改，仍是可用的 8×9 v1 宠物。
- 现有 9 个标准动画行已保留，逐帧结构检查通过，`qa/review.json` 无错误。
- 四个方向锚点 `000 up`、`090 right`、`180 down`、`270 left` 已生成并通过提取与边缘检查。
- 修复后的前半圈 `look-row-9` 已通过注册和初步语义检查，身体、尾巴、比例与基线保持稳定。
- `look-row-10` 未获得可验收候选，因此未打包、未安装 `spriteVersionNumber: 2`。

## 重大失败证据

首个完整候选在 `157.5 -> 180` 处出现明显身体姿态跳变：

- 身体中心移动约 `23.9px`
- 可见面积比约 `1.21`
- 相邻差异为 `7774` 像素，显著高于循环中位数 `1899`
- 标注方向表中可见 `157.5` 过度横躺，而 `180` 突然回正

该问题属于 `hatch-pet` 方向验收策略中的 major continuity failure，不能通过警告或人工覆盖接受。

## 已尝试的收敛策略

1. 首次独立生成后半圈：能表达左半圈方向，但与前半圈末帧发生重大中心、面积和姿态跳变。
2. 锁定身体后重新生成前半圈：成功消除前半圈整体滚转，修复行已保留。
3. 基于修复前半圈重新生成后半圈：工作器自检仍发现整身比 `157.5` 更直立，拒绝候选。
4. 把修复前半圈作为图像编辑目标，只修改头眼：工作器自检仍发现身体重绘和注册漂移，拒绝候选。
5. 用 `157.5` 单帧编辑重建连续的 `180 down` 锚点：两次候选均重绘躯干、产生注册漂移，且正下语义不够明确，未选择输出。

同一根因在提示强化和编辑策略下重复出现，继续使用当前内置图像生成路径无法满足重大连续性门槛。

## 保留的复核材料

- 标准动作检查：`qa/review.json`
- 标准动作联系表：`qa/contact-sheet.png`
- 标准动作预览：`qa/previews/`
- 注视机制：`qa/look-mechanics.md`
- 修复后的前半圈：`qa/look-row-9-registered.png`
- 被拒绝的完整 v2 候选：`qa/rejected/candidate-v2/`
- 被拒绝的后半圈生成与编辑候选：`qa/rejected/row10-generation-1.png`、`row10-generation-2.png`、`row10-edit.png`

## 继续所需条件

需要一个能够在多帧编辑中严格锁定身体注册的视觉生成能力，或由用户提供/批准一条身体姿态一致的 8 帧 `look-row-10` 源图。获得后仍需重新执行完整装配、唯一一次最终 despill、v2 校验、三方盲测与独立最终视觉 QA，才可安装。
