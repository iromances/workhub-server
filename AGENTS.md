# AGENTS.md

本文件是 `workhub-server` 仓库的 AI 入口，只说明项目定位、职责边界和文档路由。业务线现状、需求文档和研发规则按下文渐进读取，不在根文件重复维护。

## 0. AI 操作约束

- 禁止 AI 自行启动应用；需要应用运行时，必须由用户启动，或先获得用户针对本次启动的明确授权。
- 使用中文；未经用户授权，不安装软件或脚本。

## 1. 项目定位与职责

WorkHub 是公司内部研发协作与受控运维平台的后端，不是任一具体业务线的业务系统。后端基于 Java 25、Spring Boot 4、MyBatis 和 MySQL，前端仓库位于同级 `../workhub-web`。

WorkHub 主要负责：

- 研发管理：业务线、项目、迭代、版本、需求接收、研发分析、工作项、待办、附件和跟踪记录。
- 平台管理：账号、角色、权限、系统配置、AI Provider、AI 场景、操作审计和站内通知。
- 受控运维：通过 MCP 为 AI 提供业务线定位、GitLab 代码工作区、知识库上下文、数据库只读查询、服务状态和日志读取能力。
- 运维监测：XXL-JOB、系统异常、慢 SQL、账务监测以及可选 ELK 日志链路。
- 集成配置：企业微信、GitLab、AI 服务和支付渠道、商户、参数、项目绑定；支付域只管理接入配置，不承担真实支付交易编排。

具体业务系统代码统一位于 `/Users/aslight/IDEAWorkspace/{gitlabGroupName}` 下各自仓库；代码分析、需求研发和测试都使用该工作区，不在 WorkHub 仓库内维护业务代码副本。

## 2. 文档分层与命名

```text
docs/
├── 业务线/
│   ├── AGENTS.md
│   └── {业务线编号}-{业务线名称}/
│       ├── 系统现状.md
│       └── {需求编号}-{需求名称}/
│           └── 按本次选择的原有流程或 SDD 流程生成需求文档
└── 研发规范/
    ├── AGENTS.md
    ├── 业务运维分析方法.md
    ├── 通用约束.md
    ├── 研发要求与约定.md          # 流程导航
    ├── 研发设计规范.md            # 流程导航
    ├── 原有流程/
    └── SDD流程/                  # SDD 工作流、阶段指令及模板
```

- 业务线目录使用 WorkHub 的 `businessLineCode-businessLineName`，例如 `BL000001-保费分期`；不使用数据库主键、GitLab 组名或自行编制的编号。
- 需求目录直接位于所属业务线目录下，使用系统 `approvalCode`（需求/审批编号）和 `requirementName`（为空时使用 `approvalTitle`）。不得增加一层“需求”目录，不使用 `intakeId` 或“无编号”代替真实编号。
- 名称、归属及编号先通过 WorkHub MCP 或系统配置核实；无法唯一定位时先补齐信息，不猜测建目录。详细归档规则见 [业务线文档指引](docs/业务线/AGENTS.md)。
- `系统现状.md` 维护该业务线已核实的系统范围、业务规则、接口、数据模型与运行方式，记录环境、分支、提交和核验时间。需求目标、方案、测试与交付记录放在对应需求目录，不将计划写成现状。
- 原有流程在需求目录下维护 `研发方案.md`、`测试用例.md`；SDD 流程在同一需求目录的 `SDD/` 下维护规格与验证记录，具体文件依所选流程规定。

## 3. 按任务选择首读文档

| 用户任务 | 首读文档 | 需要时继续读取 |
| --- | --- | --- |
| 了解某业务线的系统、业务规则、接口或数据模型 | `docs/业务线/AGENTS.md` | 确认业务线后读取对应 `系统现状.md`，再按问题核对目标分支源码 |
| 分析某业务线、业务代码、生产数据、服务状态或日志 | `docs/研发规范/业务运维分析方法.md` | 对应业务线 `系统现状.md`、知识库、目标分支代码和 MCP 资源 |
| 了解 WorkHub 自身架构、接口、模块或集成 | 根 `pom.xml`、相关 `workhub-*` 模块 | 只读定位相关 Controller、DTO、Service、Mapper 和配置，按实际代码回答 |
| 查询或修改 MCP 工具、资源、安全边界 | `docs/研发规范/业务运维分析方法.md` | `workhub-mcp-server` 对应工具及 WorkHub MCP 资源配置；变更前选择研发流程 |
| 开发需求、修复 Bug、调整业务系统代码或编写正式方案 | `docs/研发规范/AGENTS.md` | 先询问并确认本次使用原有流程或 SDD 流程；再读通用约束及所选流程入口、对应需求目录 |
| 测试、验证或交付某个开发需求 | `docs/研发规范/AGENTS.md` | 同样先确认本次流程；只读取所选流程的验证规则和需求产物 |
| 数据库结构、Flyway、迁移失败或数据脚本 | `docs/研发规范/通用约束.md` | 目标迁移脚本和配置；若涉及开发或变更，先通过研发规范入口选择流程 |
| ELK、Filebeat、Kibana、系统异常或慢 SQL | `docs/研发规范/业务运维分析方法.md` | 相关源码和配置；生产状态通过对应业务线及环境的受控 MCP 资源核对 |
| 回看某个需求的方案或测试结果 | `docs/业务线/AGENTS.md` | 定位 `docs/业务线/{业务线编号}-{业务线名称}/{需求编号}-{需求名称}/`，按需核对 `系统现状.md` 和目标分支代码 |

业务系统开发同时涉及业务线定位和研发流程时，先读 `docs/研发规范/业务运维分析方法.md` 确认业务线、环境、分支和仓库，再读 `docs/研发规范/AGENTS.md`。本次用户未指定流程时，必须先问“本次使用原有流程还是 SDD 流程？”，得到答复前不开始正式方案或代码实施；不得从历史选择或现有文件推断默认流程。

用户明确要求的目录整理、文档路径修正和 AI 指引维护，可以直接按授权处理，无需选择研发流程；如转入业务开发、正式需求方案或需求验证，再执行上述流程选择。

## 4. 渐进式披露原则

1. 先根据用户任务从上表选择一个首读文档，不要一开始扫描整个 `docs/`。
2. 只在首读文档指向、任务证据不足或涉及第二个明确主题时，继续读取下一份文档。
3. 只有已经定位具体业务线、需求和环境后，才读取该业务线的 `系统现状.md` 相关章节及对应需求文档；不要无差别扫描其他业务线。
4. 历史方案和测试记录用于理解当时决策，不能单独证明当前实现；当前结论应结合目标环境、目标分支代码、配置、日志和只读数据核对。
5. 文档冲突时，明确区分稳定规则、当前系统事实、历史记录和推断，不把未核实内容包装成事实。

## 5. 文档维护

- 业务线系统能力、接口、数据模型或运行方式变化并完成核验后，更新对应 `系统现状.md`，记录证据与核验范围；未经核验的设计和计划留在需求文档中。
- 长期研发规则或设计要求变化时，更新 `docs/研发规范/` 下的通用层或所选流程；不要把一套流程的专属规则写进另一套。
- 每个需求的正式产物统一沉淀到 `docs/业务线/{业务线编号}-{业务线名称}/{需求编号}-{需求名称}/`；跨业务线需求在系统归属业务线下保留唯一主目录，在相关业务线现状中引用，避免多份方案分叉。
- 业务线或需求更名时按稳定编号定位已有目录，核实名称后重命名并更新链接，不按新名称重复建档。
- 只有项目定位、职责边界、文档分层或任务路由变化时，才更新本文件。
