# AGENTS.md

## 1. 目的

本文件为代码智能体提供在本仓库中工作的最小必要上下文。

- 把本文件视为项目地图，不要把它当成百科全书。
- 更细的业务规则、架构边界、接口范围和实施路线，以 `docs/` 下文档为准。
- 如果本文件与用户当前指令冲突，以用户当前指令为准。

## 2. 先读哪些文档

开始任何中等及以上规模修改前，先阅读并对照以下文档：

1. `README.md`
2. `docs/project-charter.md`
3. `docs/backend-architecture.md`
4. `docs/implementation-roadmap.md`
5. `docs/研发设计规范.md`（涉及中等及以上研发需求、接口/页面/数据/批处理/外部集成改造时）

使用原则：

- `README.md` 提供仓库入口、当前已落地能力和启动方式。
- `project-charter.md` 是业务范围、防飘逸约束和生命周期基线的最高优先级文档。
- `backend-architecture.md` 规定包结构、数据模型、API 分组和集成原则。
- `implementation-roadmap.md` 规定当前阶段目标、里程碑和验收基线。
- `研发设计规范.md` 规定开发前方案输出结构；触发范围内的需求必须先形成可审核方案，确认后再改代码或执行会改变项目状态的命令。

## 3. 项目概览

这是 `workhub-server` 后端仓库，基于 Java 25 + Spring Boot 4 + MyBatis + MySQL，用于承载：

- 本地账号密码登录和 JWT 会话
- 项目、迭代、版本、工作项、跟踪记录、状态流转 API
- 企业微信入站收件和原始消息落库
- AI 整理草稿编排
- 群机器人通知、附件处理和统一 API 输出

当前代码仍处于“最小可运行骨架向业务 API 补齐”的阶段。做功能判断时，优先看代码和 `docs/implementation-roadmap.md`，不要默认某能力已经完整落地。

关联前端项目：

- 前端仓库是同级目录下的 `../workhub-web`

## 4. 不可违背的业务约束

修改前必须确认没有越界：

- 所有外部来源数据先进入待整理箱，再由人工确认生成正式工作项。
- AI 只能生成整理草稿，不能绕过人工确认直接写正式数据。
- 每个工作项必须归属一个项目，运维事项也不例外。
- 正式工作项从 `待澄清` 开始流转，`待整理` 只用于收件箱和草稿。
- 状态变更、指派、转版本、转迭代等动作必须走明确业务动作接口，不要把这些字段当普通表单字段直接保存。
- 未更新文档前，不新增工作项类型、不改主状态、不引入审批流/权限流/组织流，不引入新的基础设施。
- 前端与第三方系统的交互统一经由后端 API，不在前端直连外部服务。

如果需求明显超出上述边界，先更新文档，再改代码。

## 5. AI 回答业务线问题的资源定位流程

当用户询问某个业务线的问题时，先把问题归类为：代码理解、需求/业务口径、数据库数据核查、服务器/日志排查或综合排查。中等及以上问题按以下顺序定位资源，不要凭记忆猜测资源位置。

### 5.1 先识别业务线和环境

- 如果用户没有明确业务线，先从上下文、需求名称、项目名称、系统名称中推断候选业务线；无法确认时，先反问用户。
- 如果问题涉及数据、服务状态、日志或生产现象，必须同时明确环境，例如 `test`、`pre`、`prod`。无法确认环境时，先反问用户。
- 业务线的标准来源是 WorkHub 中维护的业务线配置：`/api/projects/business-lines`、`pm_business_line` 或 MCP 的 `list_mcp_targets` 返回结果。
- 业务线名称、GitLab 组名、业务系统名称不是同一个概念：业务线以 `businessLineName` 为准，GitLab 代码入口以 `gitlabGroupName` 为准，系统范围以“业务线涉及系统清单 + 全局中台系统清单”为准。

### 5.2 找业务线代码

- 本仓库 `workhub-server` 只代表 WorkHub 后端自身；用户问其他业务线系统代码时，不要默认当前仓库就是目标业务系统代码。
- 业务线远端代码入口来自项目管理中的业务线 `gitlabGroupName`，配合系统配置 `gitlab.global.webApiUrl`、`gitlab.global.accessToken` 和 `gitlab.global.sshHost` 获取该 GitLab group 及子组下的仓库。
- 如果后端已经执行过研发分析，本地业务线代码缓存通常位于 `data/git-cache/group-*`，该目录只作为 GitLab 拉取后的隔离缓存使用。使用缓存前应确认它对应的业务线、仓库 URL 和最近 fetch 状态；不要把任意同级目录当成业务线代码。
- 如果本地缓存不存在或明显过期，优先通过已配置的 GitLab 远端信息刷新/获取；如果缺少 GitLab 配置或权限，明确告诉用户需要先在 WorkHub 中补齐业务线 GitLab 组名和全局 GitLab 配置。
- 回答代码问题时说明使用的是本地缓存还是远端仓库，并尽量标明仓库、分支、提交或文件路径；无法确认版本时明确说明。

### 5.3 找知识库文档

- 项目知识库根目录来自系统配置 `knowledge.project.vaultPath`，默认是 `/Users/aslight/Obsidian Vault/Company Obsidian Vault`。
- 检索知识库时必须先用业务线中文名圈定范围，再用项目名、系统名、审批编号、需求名称、业务关键词收敛范围。
- 优先读取相关 Markdown/TXT 文档、需求迭代文档和原始材料归档路径，不要无差别扫描整个知识库。
- 需求评估生成的知识库路径约定为：
  - `raw/requirements/{业务线}/{项目名称}/{年份}/{审批编号}/source.md`
  - `wiki/projects/{业务线}/{项目名称}/需求迭代/{年份}/{审批编号}-{需求名称}.md`
- 知识库内容只能作为业务口径和历史背景证据；如果文档与代码、数据库或用户当前描述冲突，要明确指出冲突并说明采用哪一类证据。

### 5.4 找数据库和服务器资源

当问题需要访问数据库或服务器时，必须遵守以下约束：

- 先通过 MCP 工具 `list_mcp_targets` 查看已配置的业务线、环境、数据库目标和服务器目标，按 `businessLineCodes + environmentCode + targetKey + profileKey` 选择最小范围。
- 数据库访问只使用 MCP `run_readonly_query`，默认选择只读 profile，例如 `readonly`；禁止绕过 MCP 直连数据库、SSH 隧道、堡垒机或手工使用连接串。
- SQL 必须是本次问题所需的最小只读查询，禁止写入、DDL、多语句、导出、高风险函数和无边界全表扫描；查询前先说明目的，查询后基于结果回答。
- 服务器访问只使用 MCP `get_service_status` 和 `read_service_logs`，只能读取白名单服务状态、白名单服务日志或白名单日志路径；禁止要求或执行任意 shell 命令。
- 如果对应业务线、环境或资源类型尚未配置 MCP 资源，先提示用户在 WorkHub 中新增 MCP 资源配置，再继续排查。
- 所有通过 MCP 的数据库和服务器访问都应保留审计记录，便于事后追踪。
- 不要在回答中暴露数据库密码、Access Token、SSH 密码、私钥、堡垒机凭据或本地 catalog 中的敏感字段。

### 5.5 回答业务线问题的输出要求

- 先给结论，再列支撑依据；把“代码证据、知识库证据、数据库结果、服务器日志、个人推断”区分清楚。
- 如果只查到了部分资源，要说明缺口，例如缺少 GitLab 配置、知识库路径不存在、MCP 未配置该环境、服务器日志白名单不足。
- 对生产问题，默认只做诊断和只读核查；任何修复、重启、数据修正、DDL/DML、配置变更都必须等用户明确确认，并且优先通过系统已有业务接口或受控运维流程执行。
- 回答中不要把 AI 生成内容包装成已确认事实；没有证据时使用“推测”“需要确认”等表述。

## 6. 仓库结构

重点目录：

- `workhub-model/`：按业务域分组的请求、响应、实体和值对象
- `workhub-support/`：配置属性模型
- `workhub-dao/`：按业务域分组的 MyBatis 数据访问接口
- `workhub-service/`：按业务域分组的业务动作、集成服务与 JWT 令牌服务
- `workhub-controller/`：按业务域分组的 HTTP 接口、统一响应、异常处理
- `workhub-job/`：定时任务
- `workhub-bootstrap/`：Spring Boot 启动入口、运行时配置与资源
- `workhub-bootstrap/src/main/resources/db/schema/mysql/`：初始化 SQL
- `docs/`：业务、架构、路线图等真实规则来源

约定：

- Controller 不写核心业务编排。
- Service 按业务动作拆分，不写超长万能方法。
- DAO 只负责数据访问，不承载业务规则。
- 表结构变更时，同步更新 SQL 和相关文档。
- 新增 API 时，优先按 `auth/project/sprint/release/workitem/intake` 等业务域归档到 `controller/`、`model/`、`dao/`、`service/`。

## 7. 修改代码时的工作方式

1. 先确认变更属于当前路线图阶段范围。
2. 涉及新功能、页面改造、接口改造、数据库结构变更、数据修复、定时任务、批处理或外部接口改造时，先按 `docs/研发设计规范.md` 输出方案；方案审核通过前，不修改代码、不执行改变项目状态的命令。
3. 先找已有文档和现有实现，再决定是否新增包或抽象。
4. 小改动尽量复用既有模式；中大型改动优先补齐对应领域的 `controller`、`model`、`dao`、`service`。
5. 改业务接口时，同时检查：
   - 是否需要新增请求/响应模型
   - 是否需要新增或扩展 DAO
   - 是否需要同步更新 SQL 或文档
   - 是否需要为前端补充稳定的响应字段
6. 如发现文档与实现不一致：
   - 若代码明显只是骨架，按文档为准推进
   - 若准备改变既有规则，先更新文档

## 8. 代码风格与实现偏好

- 使用 Java 25。
- 继续沿用 Spring Boot + MyBatis 注解/分层模式，保持实现直接、清晰。
- Controller、Model、DAO、Service 统一按业务域分层组织，不把跨领域逻辑塞进单个类。
- 明确动作接口优先于“大而全 update”。
- 关键写操作优先保留留痕能力，尤其是工作项状态流转和跟踪记录。
- 演示数据只允许作为临时骨架；一旦进入 M3 及之后阶段，优先接入真实数据库查询。

## 9. 常用命令

- 编译验证：`mvn -q -DskipTests compile`
- 本地启动：`mvn -pl workhub-bootstrap spring-boot:run`

当前仓库未看到成体系测试代码。提交较大改动前，至少保证：

- `mvn -q -DskipTests compile` 通过
- 受影响接口完成基本手工验证

## 10. 文档维护要求

当你做以下变更时，应同时更新对应文档：

- 新增或移除核心 API 分组
- 改变业务范围、对象模型或生命周期
- 引入新的全局交互规则或集成方式
- 调整实施阶段、验收基线或开发顺序

优先保持 `AGENTS.md` 简短稳定；详细说明写入 `docs/`，再从这里链接过去。

## 11. 交付预期

完成任务时，尽量说明：

- 改了哪些文件
- 影响了哪个业务域
- 是否更新了文档
- 跑了哪些验证命令
- 还有哪些未完成项或依赖后端/前端联调

如果以后仓库出现多个相对独立的子模块，可在子目录补充更近一级的 `AGENTS.md`，就近覆盖本文件中不适用的部分。
