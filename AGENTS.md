# AGENTS.md

本文件是 `workhub-server` 仓库的 AI 入口，只说明项目定位、职责边界和文档路由。具体事实、规则、研发流程和历史记录按下文渐进读取，不在根文件重复维护。

## 0. AI 操作约束

- 禁止 AI 自行启动应用；需要应用运行时，必须由用户启动，或先获得用户针对本次启动的明确授权。

## 1. 项目定位与职责

WorkHub 是公司内部研发协作与受控运维平台的后端，不是任一具体业务线的业务系统。后端基于 Java 25、Spring Boot 4、MyBatis 和 MySQL，前端仓库位于同级 `../workhub-web`。

WorkHub 主要负责：

- 研发管理：业务线、项目、迭代、版本、需求接收、研发分析、工作项、待办、附件和跟踪记录。
- 平台管理：账号、角色、权限、系统配置、AI Provider、AI 场景、操作审计和站内通知。
- 受控运维：通过 MCP 为 AI 提供业务线定位、GitLab 代码缓存、知识库上下文、数据库只读查询、服务状态和日志读取能力。
- 运维监测：XXL-JOB、系统异常、慢 SQL、账务监测以及可选 ELK 日志链路。
- 集成配置：企业微信、GitLab、AI 服务和支付渠道、商户、参数、项目绑定；支付域只管理接入配置，不承担真实支付交易编排。

具体业务系统代码位于 `/Users/aslight/Documents/workspace/IDEAWorkspace` 下各自仓库。`data/git-cache` 只是 WorkHub 受控只读代码缓存，不是业务系统开发工作区。

## 2. 文档分层

- `docs/事实/`：当前系统事实、接口、数据模型、能力边界和运维方法。回答“现在是什么、怎么运行、有哪些能力”时优先读取。
- `docs/规则与约定/`：长期研发规则、设计规范和安全约束。准备修改代码或方案时按需读取。
- `docs/研发/`：研发流程、测试规则、模板和按需求聚合的研发产物。开发、修复或测试需求时进入。
- `docs/事实/业务线/`：特定业务线的长期专题事实。必须先确认业务线，再读取对应业务线和问题相关的文档。

## 3. 按任务选择首读文档

| 用户任务 | 首读文档 | 需要时继续读取 |
| --- | --- | --- |
| 了解项目架构、模块、数据模型、安全或集成 | `docs/事实/系统架构与设计.md` | `docs/事实/控制器接口说明.md`、相关源码 |
| 查询接口、请求字段、返回字段或页面对应 API | `docs/事实/控制器接口说明.md` | `docs/事实/系统架构与设计.md`、Controller 和 DTO 源码 |
| 分析某业务线、业务代码、生产数据、服务状态或日志 | `docs/事实/业务运维分析方法.md` | 对应业务线知识库、目标分支代码、MCP 资源和 `docs/事实/业务线/` 专题 |
| 查询或修改 MCP 工具、资源、安全边界 | `docs/事实/MCP能力与安全边界.md` | `docs/事实/系统架构与设计.md`、`docs/事实/控制器接口说明.md` |
| 开发需求、修复 Bug 或调整业务系统代码 | `docs/研发/AGENTS.md` | `docs/规则与约定/研发要求与约定.md`、`docs/规则与约定/研发设计规范.md`、对应需求目录 |
| 测试或验证某个需求 | `docs/研发/研发测试.md` | 对应 `docs/研发/需求/{需求编号}-{需求名称}/测试用例.md` |
| 数据库结构、Flyway、迁移失败或数据脚本 | `docs/事实/数据库迁移管理.md` | `docs/规则与约定/研发要求与约定.md`、目标迁移脚本 |
| ELK、Filebeat、Kibana、系统异常或慢 SQL | `docs/事实/ELK日志监控.md` | `docs/事实/系统架构与设计.md`、`docs/事实/控制器接口说明.md` |
| 回看某个需求的方案或测试结果 | 对应 `docs/研发/需求/{需求编号}-{需求名称}/` | 当前事实文档和目标分支代码 |
| 需求字段拆分专题 | `docs/事实/需求fields字段拆分建议.md` | 当前数据模型、接口和相关需求方案 |

业务系统开发同时涉及业务线定位和研发流程时，先读 `docs/事实/业务运维分析方法.md` 确认业务线、环境、分支和仓库，再读 `docs/研发/AGENTS.md` 进入研发流程。

## 4. 渐进式披露原则

1. 先根据用户任务从上表选择一个首读文档，不要一开始扫描整个 `docs/`。
2. 只在首读文档指向、任务证据不足或涉及第二个明确主题时，继续读取下一份文档。
3. 只有已经定位具体业务线、需求和环境后，才读取对应专题及需求目录中的研发方案和测试记录。
4. 历史方案和测试记录用于理解当时决策，不能单独证明当前实现；当前结论应结合目标环境、目标分支代码、配置、日志和只读数据核对。
5. 文档冲突时，明确区分稳定规则、当前系统事实、历史记录和推断，不把未核实内容包装成事实。

## 5. 文档维护

- 系统能力、接口、数据模型或运行方式变化时，更新 `docs/事实/` 下对应文档。
- 长期研发规则或设计要求变化时，更新 `docs/规则与约定/`。
- 每个需求的研发方案、测试用例和专属辅助材料统一沉淀到 `docs/研发/需求/{需求编号}-{需求名称}/`。
- 只有项目定位、职责边界、文档分层或任务路由变化时，才更新本文件。
