# AGENTS.md

本文件是 `workhub-server` 仓库的 AI 协作规则入口。它只保留项目定位和文档入口；详细业务、架构、接口、研发要求和专题等说明以 `docs/` 下文档为准。

## 1. 项目概览

这是 WorkHub 后端仓库，基于 Java 25、Spring Boot 4、MyBatis、MySQL，用于承载：

- 公司各业务线需求管理与系统研发
- 面向 AI 客户端的受控 MCP 只读运维通道。

关联前端仓库在同级目录 `../workhub-web`，可读写。

## 2. 业务线问题处理原则

当用户询问“某业务线/某项目问题时，必须先按 `docs/事实/业务运维分析方法.md` 解析业务线到 WorkHub 配置中的 `gitlabGroupName` 和受控代码缓存，没有缓存就从远端仓库同步，再限定到该 GitLab group 下的仓库检索。

业务线当前业务口径和专题资料必须通过 MCP `get_knowledge_context` 或 `list_mcp_targets` 定位 WorkHub 配置的知识库，再从对应业务线索引渐进读取；不得硬编码知识库根路径或无差别扫描整个知识库。接口文档只作为契约参考，代码、配置、日志和生产数据结论仍按目标环境与分支核对。具体阅读顺序见 `docs/事实/业务运维分析方法.md`。

开始排查前必须确认本次问题对应的环境，并按环境确定代码分支：环境包括生产、开发、测试等，方便使用 MCP 提供的 DB 或 SERVER 对应环境。用户未另行指定分支时，生产环境默认使用 `master`，无需再向用户确认分支；测试环境默认使用该环境当前发版分支，无法从部署信息确认时使用需求关联的开发分支，也无需再向用户确认。只有目标环境未明确，或者非生产、测试环境的分支无法确定，或者测试环境既无法解析当前发版分支也没有需求关联开发分支时，才向用户补充确认；完成环境和分支解析前不得开始代码、配置、日志或数据库排查。排查一开始必须先输出当前业务线、GitLab 项目组、环境、解析后的分支及其来源，确保后续结论都有明确上下文。

当前系统 `pm_business_line` 已维护的业务线和 GitLab 组名如下；如系统配置发生变化，同步修改此处维护的数据。

| 业务线编码 | 业务线名 | gitlabGroupName |
| --- | --- | --- |
| BL000001 | 保费分期 | scf |
| BL000009 | 嘉泰租赁资产保理 | ca-assets |
| BL000008 | 叉车 | lease-saas |
| BL000003 | 嘉泰消费分期 | th-jiatai-amp |
| BL000005 | 智能柜 | ol |
| BL000010 | 服务费分期保理 | ca-assets |
| BL000007 | 嘉泰设备租赁 | th-jiatai-equip |
| BL000006 | 物流平台 | xxt-plateform |
| BL000002 | 账单管理 | ca-assets |
| BL000004 | 消费分期 | asset-plateform |

禁止在未确认业务线配置前，因为同级目录仓库名、关键词命中或历史经验相似，就直接分析任意业务仓库并给出结论。

嘉泰消费分期前端仓库特别注意：`jiatai-amp-opadmin` 是嘉泰当前运营后台前端；`jiatai-amp-admin` 是演示用旧系统。除非用户明确点名 `jiatai-amp-admin`，嘉泰消费分期业务前端需求不要改到 `jiatai-amp-admin`。

### 2.1 消费分期与嘉泰消费分期跨业务线规则

消费分期业务线（`BL000004`，GitLab 组 `asset-plateform`）的部分项目采用再保理模式，嘉泰消费分期业务线（`BL000003`，GitLab 组 `th-jiatai-amp`）是相关再保理链路的资方侧，因此两条业务线之间存在订单、支付、核销、放款和状态同步等跨系统关联。

处理消费分期或嘉泰消费分期的问题分析、开发、需求梳理和测试任务时，必须先判断是否涉及再保理模式或上述跨系统链路；只要涉及，就必须同时纳入 `BL000004` 与 `BL000003` 两条业务线，分别解析两边的关联系统、GitLab 项目组、代码仓库、目标环境、开发分支、SERVER/DB 资源、服务入口、测试数据和上下游影响，不得只根据需求主业务线、主仓库或单侧接口得出结论。

跨业务线任务开始时必须明确输出两条业务线各自涉及的系统和资源边界。开发时应区分两边仓库的改动归属；需求梳理时应覆盖两边状态流转和接口契约；测试时应覆盖消费分期业务侧、嘉泰资方侧以及跨系统同步结果。只有在结合需求、代码和业务配置确认任务不涉及再保理或跨系统链路后，才可以限定为单业务线处理。

## 3. 业务线开发工作原则

当用户要求处理某业务线的开发工作时，必须先按上一节确认业务线、项目和对应系统，再定位到本机 `/Users/aslight/Documents/workspace/IDEAWorkspace` 下的对应系统仓库开展开发，没有对应系统时从远端仓库同步到该目录下。

如果需求同时涉及 WorkHub 自身能力与业务系统代码，应明确区分改动边界：WorkHub 平台能力改在本仓库或同级 `workhub-web`，业务系统功能改在 IDEAWorkspace 下确认后的对应系统仓库。

### 3.1 业务系统代码开发指引

- 受控代码缓存 `data/git-cache/{gitlabGroupName}` 只用于从远端同步、刷新和只读查看，包括分支确认、代码检索、差异分析与问题定位。禁止在受控缓存仓库及其子目录中修改业务代码、创建开发分支或工作树、提交代码，亦不得将其作为业务系统开发工作区。
- 修改业务系统代码前，必须先确定本次开发对应的具体需求，至少确认需求编号或名称、关联业务线、关联系统、目标环境和开发分支。处理 Bug 时还必须从 Bug 关联的需求、迭代、版本或提测构建反查所属需求；无法确定具体需求时不得开始修改，应先补齐需求归属。
- 需求和系统确认后，必须到 WorkHub 仓库同级目录 `/Users/aslight/Documents/workspace/IDEAWorkspace/{系统仓库名}` 定位对应系统仓库，并校验其 Git 远端属于已确认业务线的 `gitlabGroupName`。不得因为受控缓存中已经存在仓库，就直接在缓存中开发。
- 同级目录不存在对应系统仓库时，应将已确认的远端仓库同步到 `/Users/aslight/Documents/workspace/IDEAWorkspace` 下再开展开发；不得以复制受控缓存、在受控缓存下新增工作树等方式替代正式业务系统仓库。
- 开始修改前必须先输出并确认本次需求、业务线、GitLab 项目组、涉及系统、正式本地仓库路径、开发分支及分支来源。需求涉及多个系统时，应逐一列明代码改动归属，并分别在对应的同级系统仓库中修改和验证。
- WorkHub 自身后端和前端不适用业务系统缓存开发流程：后端修改位于本仓库，前端修改位于同级 `../workhub-web`；仍须先明确需求和改动边界。

## 4. AGENT测试指引

当用户要求“测试某需求”“验证某需求是否有问题”“按需求编号/名称/摘要做测试”时，必须先把测试上下文补齐，再执行接口、页面、日志或数据库验证。测试上下文优先通过 WorkHub MCP 能力获取；MCP 缺失或能力不足时，必须明确说明缺口，并向用户补要必要信息，不得用猜测的环境、分支、服务或数据直接测试。

### 4.1 MCP前置上下文

以下上下文应优先通过既有 WorkHub MCP 能力查询确认；其中需求已关联业务线，SERVER 和 DB 资源也已与业务线建立关系，测试时应优先从需求解析出的业务线出发，继续解析对应环境下的服务器、数据库、服务入口和代码仓库，不另行臆造一套环境或系统映射：

- 需求定位：通过需求编号、名称、摘要或关键词定位需求详情、验收标准、关联设计、测试用例、附件、关联分支和关联系统。
- 业务线解析：从需求详情读取关联业务线；没有关联业务线或业务线不明确时，必须先向用户确认，不得按仓库名或关键词猜测。
- 环境资源解析：按需求业务线和目标环境查询既有 SERVER、DB MCP 资源关系，确认可用服务器、数据库、服务入口、网关前缀、必要 header 和认证方式。
- 分支与部署确认：结合需求关联分支、代码仓库和 SERVER 部署信息，确认涉及服务当前部署的分支、commit、镜像版本、发布时间，以及需求分支是否已部署到目标环境。用户未另行指定时，生产环境按 `master` 处理；测试环境优先使用当前发版分支，无法确认时使用需求关联的开发分支。
- 业务线系统映射：确认业务线编码、业务线名称、GitLab group、涉及服务、仓库和本地路径；系统入口优先以业务线关联 SERVER 资源为准。
- 测试入口：确认目标环境的网关域名、服务前缀、是否允许直连服务、直连地址、必要 header 和认证方式。
- 测试数据：按需求、业务线、环境和场景获取可复用测试数据，包括申请单、客户、银行卡、协议号、账单、订单、短信验证码规则和允许写入的数据池。
- 测试安全策略：确认哪些接口允许写数据、是否允许触发短信、是否允许真实支付/扣款、是否需要清理数据、哪些场景只能做负向或只读验证。

### 4.2 测试资源与执行责任

- SERVER 资源：业务线在 WorkHub MCP 中关联的目标环境 SERVER 即该业务线的权威测试服务器。不得因为同级仓库配置、历史部署地址或经验另行猜测服务器；服务状态查询返回 systemd unit 不存在时，只能说明服务单元名或运行方式不匹配，不能据此否定 SERVER 配置，应继续结合代码分支中的部署目录、启动脚本、进程方式和日志路径定位服务。
- 环境地址：运营端、用户端、合作方接口、网关等环境 URL 和服务前缀统一从业务线的对应环境配置读取。配置存在时直接使用，不再向用户重复询问；配置缺失或不足以组成完整请求入口时，明确指出缺少的业务线环境地址或网关字段，等待配置补齐，不得从代码中的历史地址、构建输出或相似业务线推断正式测试入口。
- 临时鉴权：接口鉴权信息由用户在每次测试任务中临时提供，包括 token、签名和必要 header 等。不得要求将临时凭证长期保存到 WorkHub、代码、文档或测试报告；测试输出不得回显凭证明文。未取得本次有效鉴权信息前，可以完成只读上下文、代码、脚本和测试数据准备，但不得伪造身份发起受保护接口请求。
- 分支脚本与配置：涉及数据库结构、字典、业务配置或初始化数据时，先检查需求关联代码分支中的 DDL、DML、迁移脚本和配置脚本，并与目标环境只读查询结果核对是否已执行。分支没有对应脚本时，应继续判断该配置是否由程序自动生成、配置中心维护或需要人工维护，并将缺口记录为提测问题，不得默认要求用户手工准备，也不得直接写库补数据。
- 测试数据准备：测试数据原则上由 AI 从目标测试环境通过受控只读 MCP 查询并筛选，或在已确认安全策略后通过正常业务接口准备；用户无需默认手工提供普通测试数据。只有在无法安全复用或创建数据、缺少必要业务授权、数据会触发真实外部影响，或现有 MCP 无法查询到关键数据时，才向用户补要具体数据或授权。数据库 MCP 始终只读，不得通过数据库连接新增、修改或清理测试数据。
- 下游链路：通知、账户、支付、账单核销、清分及其他下游调用按程序自身的正常入口链路自动触发，测试时不默认要求用户逐项提供下游调用参数或 Mock。应从入口请求出发，通过接口返回、服务日志、只读数据库和下游状态验证完整链路；只有实际出现下游不可用、环境指向真实外部系统或安全边界不明确时，才将下游配置、隔离或授权列为阻塞。高风险正向链路仍须遵守下一节的安全确认要求。

### 4.3 测试执行原则

- 开始测试前必须输出当前需求、需求关联业务线、涉及系统、环境、分支、SERVER/DB 资源来源、服务入口和测试数据来源。
- 未确认目标环境，或未按默认规则、部署信息、需求关联分支或用户指定解析出目标分支前，不得开始接口、配置、日志、数据库或页面测试；生产环境默认分支为 `master`，测试环境默认分支为当前发版分支或需求关联的开发分支，不再重复向用户确认。
- 未确认测试安全策略前，不得发起可能真实落库、发短信、签约、支付、扣款、放款、退款、解绑、撤销或通知外部系统的请求。
- 对支付、绑卡、代扣、代付、放款、退款等高风险链路，优先使用只读查询、幂等同步、重复申请、错卡号、无效协议号等安全负向方式验证链路是否命中目标逻辑。
- 如必须执行正向写入链路，必须确认数据可写、可重复执行、可清理，并在测试结论中记录写入影响。
- 数据库验证只能使用受控只读 MCP 能力；没有 MCP 时不得私自直连业务数据库。
- 测试过程中不得在结论中泄露 token、密码、密钥、证件号、银行卡号、手机号等敏感信息；必要时只展示脱敏值或业务结论。

### 4.4 测试结果口径

测试结论必须区分以下状态：

- 通过：目标环境、分支和数据均已确认，核心验收点完成验证，结果符合预期。
- 部分通过：核心链路部分验证通过，但存在未覆盖场景、缺少数据、环境包不一致或只能做负向验证。
- 不通过：接口返回、数据状态、页面展示、日志或下游行为与需求预期不符。
- 阻塞：缺少环境、分支、服务入口、授权、测试数据、MCP 能力或安全授权，无法继续有效验证。

测试输出至少包含：

- 测试范围：需求、环境、分支、业务线、系统和接口/页面清单。
- 执行结果：每个场景的请求入口、关键入参类别、返回结果和是否通过。
- 发现问题：问题现象、复现条件、影响范围、疑似原因和建议修复方向。
- 卡点与补救：缺少的上下文、测试数据、安全授权或 MCP 能力，以及后续需要补齐的内容。

## 5. 文档索引

### 工具和方法

- `docs/事实/业务运维分析方法.md`：业务线代码、知识库、数据库和服务器资源的定位、排查、安全约束和输出要求。
- `docs/事实/MCP能力与安全边界.md`：WorkHub MCP 只读能力、资源来源和安全边界。

### 事实

- `docs/事实/系统架构与设计.md`：系统架构、后端模块边界、核心数据模型和 API 分组等系统/项目事实。
- `docs/事实/控制器接口说明.md`：Controller 层接口说明。
- `docs/事实/MCP能力与安全边界.md`：WorkHub MCP 只读能力、资源来源和安全边界。
- `docs/事实/ELK日志监控.md`：本地 ELK、Filebeat、Kibana 看板和日志监控说明。

### 规则与约定

- `docs/规则与约定/研发要求与约定.md`：研发要求、工程约定、安全边界、AI 协作规则和 WorkHub 项目专项业务规则。
- `docs/规则与约定/研发设计规范.md`：中等及以上需求开发前的方案输出规范。

### 模版

- `docs/模版/自动化测试用例模板.md`：自动化测试用例输出模板。


## 6. 文档维护触发

优先保持根目录 `AGENTS.md` 简短稳定；详细说明写入 `docs/`，再从入口文档链接过去。

当做以下变更时，应同步更新对应文档：

- 新增或移除核心 API 分组。
- 改变业务范围、对象模型或生命周期。
- 引入新的全局交互规则或集成方式。
- 调整业务范围、验收基线或开发顺序。
- 新增、修改或删除 MCP 工具。
- 调整业务运维分析方法、知识库路径规则或生产资源访问边界。
- 新增或修改专题能力。
