# AGENTS.md

使用中文。

本文件是 `workhub-server` 仓库的 AI 协作规则入口。它只保留必须遵守的边界、定位流程和开发约定；详细业务、架构、接口和专题说明以 `docs/` 下文档为准。

## 1. 规则来源

- `docs/project-charter.md`：业务范围、对象模型、生命周期基线和防飘逸约束，优先级最高。
- `docs/backend-architecture.md`：后端模块边界、包结构、数据模型、API 分组和集成原则。
- `docs/controller-api.md`：Controller 层接口说明。
- `docs/研发设计规范.md`：中等及以上需求开发前的方案输出规范。
- `MCP_CAPABILITIES.md`：WorkHub MCP 只读能力清单和工具边界。
- `docs/README.md`：文档索引。

如果文档与代码不一致，先判断代码是否只是骨架；准备改变既有业务规则、生命周期、对象模型或全局交互规则时，先更新对应文档。

## 2. 项目概览

这是 WorkHub 后端仓库，基于 Java 25、Spring Boot 4、MyBatis、MySQL，用于承载：

- 本地账号密码登录和 JWT 会话。
- 项目、迭代、版本、工作项、跟踪记录和状态流转 API。
- 企业微信入站收件、待整理箱、AI 整理草稿和人工确认流程。
- 需求截图/附件结构化增强、研发需求代码影响分析和知识库归档。
- 群机器人通知、附件处理、支付接入配置管理、系统配置和统一 API 输出。
- 面向 AI 客户端的受控 MCP 只读运维通道。

关联前端仓库在同级目录 `../workhub-web`。

`data/git-cache/group-*` 是业务线代码拉取缓存，不是 WorkHub 后端自身代码；缓存中的 `AGENTS.md` 只适用于对应业务线仓库，不作为本仓规则来源。

## 3. 不可违背的业务约束

- 所有外部来源数据先进入待整理箱，再由人工确认生成正式工作项。
- AI 只能生成整理草稿，不能绕过人工确认直接写正式数据。
- 每个工作项必须归属一个项目，运维事项也不例外。
- 正式工作项从 `待澄清` 开始流转，`待整理` 只用于收件箱和草稿。
- 状态变更、指派、转版本、转迭代等动作必须走明确业务动作接口，不要把这些字段当普通表单字段直接保存。
- 未更新文档前，不新增工作项类型、不改主状态、不引入审批流/权限流/组织流，不引入新的基础设施。
- 前端与第三方系统的交互统一经由后端 API，不在前端直连外部服务。
- 结构化增强只负责补充需求字段，不直接写入正式工作项。
- SQL 草稿只用于辅助人工处理数据运维需求，必须由人工确认库表、字段、权限和导出范围后自行执行。

如果需求明显超出上述边界，先更新文档，再改代码。

## 4. 仓库结构和分层

- `workhub-model/`：请求、响应、实体和值对象。
- `workhub-support/`：配置属性模型。
- `workhub-dao/`：MyBatis 数据访问接口。
- `workhub-service/`：业务动作、集成服务与 JWT 令牌服务。
- `workhub-controller/`：HTTP 接口、统一响应、异常处理。
- `workhub-job/`：定时任务。
- `workhub-mcp-server/`：面向 AI 客户端的只读运维 MCP 安全通道。
- `workhub-bootstrap/`：Spring Boot 启动入口、运行时配置和资源。
- `workhub-bootstrap/src/main/resources/db/schema/mysql/`：初始化 SQL。
- `docs/`：业务、架构、接口和研发规范等真实规则来源。

分层约定：

- Controller 不写核心业务编排。
- Service 按业务动作拆分，不写超长万能方法。
- DAO 只负责数据访问，不承载业务规则。
- 新增 API 优先按 `auth/project/sprint/release/workitem/intake/payment/ops` 等业务域归档到对应 `controller`、`model`、`dao`、`service`。
- 表结构变更时，同步更新 SQL、模型、DAO、服务逻辑和相关文档。

## 5. 修改代码前的工作方式

1. 先确认变更不违背项目宪章、后端架构和当前已落地能力边界。
2. 涉及新功能、页面改造、接口改造、数据库结构变更、数据修复、定时任务、批处理或外部接口改造时，先按 `docs/研发设计规范.md` 输出方案；方案审核通过前，不修改代码、不执行改变项目状态的命令。
3. 先找已有文档和现有实现，再决定是否新增包或抽象。
4. 小改动复用既有模式；中大型改动优先补齐对应领域的 `controller`、`model`、`dao`、`service`。
5. 改业务接口时，同时检查是否需要新增请求/响应模型、DAO、SQL、文档和前端稳定响应字段。
6. 如发现文档与实现不一致：代码明显只是骨架时按文档推进；准备改变既有规则时先更新文档。

## 6. 代码风格

- 使用 Java 25。
- 继续沿用 Spring Boot + MyBatis 注解/分层模式，保持实现直接、清晰。
- Controller、Model、DAO、Service 统一按业务域分层组织。
- 明确动作接口优先于“大而全 update”。
- 关键写操作优先保留留痕能力，尤其是工作项状态流转和跟踪记录。
- 演示数据只允许作为临时骨架；进入真实业务阶段后优先接入真实数据库查询。

## 7. 业务线问题定位流程

当用户询问某个业务线的问题时，先归类为：代码理解、需求/业务口径、数据库数据核查、服务器/日志排查或综合排查。中等及以上问题不要凭记忆/推理猜测资源位置。

### 7.1 识别业务线和环境

- 如果用户没有明确业务线，先反问。
- 如果问题涉及数据、服务状态、日志或生产现象，必须同时明确环境，无法确认时先反问。
- 业务线标准来源是 WorkHub 中维护的业务线配置：`/api/projects/business-lines`、`pm_business_line` 或 MCP 的 `list_mcp_targets` 返回结果。

### 7.2 找业务线代码

- 本仓库只代表 WorkHub 后端自身；用户问其他业务线系统代码时，不要默认当前仓库就是目标业务系统代码。
- 业务线远端代码入口来自项目管理中的业务线 `gitlabGroupName`，配合系统配置 `gitlab.global.webApiUrl`、`gitlab.global.accessToken` 和 `gitlab.global.sshHost` 获取 GitLab group 及子组仓库。
- 本地业务线代码缓存通常位于 `data/git-cache/group-*`。使用缓存前应确认它对应的业务线、仓库 URL 和最近 fetch 状态；不要把任意同级目录当成业务线代码。
- 回答代码问题时说明使用的是本地缓存还是远端仓库，并尽量标明仓库、分支、提交或文件路径。

### 7.3 找知识库文档

- 项目知识库根目录来自系统配置 `knowledge.project.vaultPath`，默认是 `/Users/aslight/Obsidian Vault/Company Obsidian Vault`。
- 检索知识库时必须先用业务线中文名圈定范围，再用项目名、系统名、审批编号、需求名称、业务关键词收敛范围。
- 优先读取相关 Markdown/TXT 文档、需求迭代文档和原始材料归档路径，不要无差别扫描整个知识库。
- 知识库内容只能作为业务口径和历史背景证据；如果文档与代码、数据库或用户当前描述冲突，要指出冲突并说明采用哪类证据。

### 7.4 查数据库和服务器

- 先通过 MCP 工具 `list_mcp_targets` 查看已配置的业务线、环境、数据库目标和服务器目标，按 `businessLineCodes + environmentCode + targetKey + profileKey` 选择最小范围。
- 数据库访问只使用 MCP `run_readonly_query`，默认选择只读 profile，例如 `readonly`；禁止绕过 MCP 直连数据库、SSH 隧道、堡垒机或手工使用连接串。
- SQL 必须是本次问题所需的最小只读查询，禁止写入、DDL、多语句、导出、高风险函数和无边界全表扫描。
- 服务器访问只使用 MCP `get_service_status` 和 `read_service_logs`，只能读取白名单服务状态、白名单服务日志或白名单日志路径；禁止要求或执行任意 shell 命令。
- 不要在回答中暴露数据库密码、Access Token、SSH 密码、私钥、堡垒机凭据或本地 catalog 中的敏感字段。

### 7.5 输出要求

- 先给结论，再列支撑依据。
- 区分“代码证据、知识库证据、数据库结果、服务器日志、个人推断”。
- 如果只查到部分资源，要说明缺口。
- 对生产问题，默认只做诊断和只读核查；任何修复、重启、数据修正、DDL/DML、配置变更都必须等用户明确确认。
- 没有证据时使用“推测”“需要确认”等表述，不要把 AI 生成内容包装成已确认事实。

## 8. 常用命令

```bash
mvn -q -DskipTests compile
mvn -pl workhub-bootstrap spring-boot:run
```

默认后端地址：`http://127.0.0.1:8080`

默认演示账号：`admin / admin123`

当前仓库未看到成体系测试代码。提交较大改动前，至少保证 `mvn -q -DskipTests compile` 通过；受影响接口应完成基本手工验证。

## 9. 文档维护

当你做以下变更时，应同步更新对应文档：

- 新增或移除核心 API 分组。
- 改变业务范围、对象模型或生命周期。
- 引入新的全局交互规则或集成方式。
- 调整业务范围、验收基线或开发顺序。

优先保持 `AGENTS.md` 简短稳定；详细说明写入 `docs/`，再从这里链接过去。

